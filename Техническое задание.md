# ТЕХНИЧЕСКОЕ ЗАДАНИЕ
## на разработку web-приложения «Цифровой чек-лист инженера по техническому обслуживанию оборудования»

---

## 1. ОБЩИЕ СВЕДЕНИЯ

### 1.1. Наименование системы
Web-приложение «Цифровой чек-лист инженера по техническому обслуживанию оборудования» (далее — Приложение).

### 1.2. Назначение системы
Автоматизация деятельности инженера по техническому обслуживанию оборудования: создание чек-листа работ, фиксация контролируемых параметров, фотофиксация состояния оборудования «до» и «после» работ, формирование отчета в формате PDF с маркированными фотографиями и отправка заказчику по email.

### 1.3. Цели проекта
- Оцифровать процесс обслуживания оборудования
- Подтвердить факт выполнения работ фотофиксацией
- Сократить трудозатраты инженера на 30–40%
- Исключить влияние человеческого фактора на полноту отчетов
- Обеспечить работу в условиях отсутствия интернета (подвалы, серверные)
- Не мешать посетителям объекта во время работ

### 1.4. Нормативные ссылки
- ФЗ-152 «О персональных данных»
- ГОСТ Р ИСО/МЭК 12207-2010 «Программная инженерия. Процессы жизненного цикла программных средств»
- СП 60.13330.2020 «Отопление, вентиляция и кондиционирование воздуха»
- ПУЭ (Правила устройства электроустановок)

---

## 2. ТЕРМИНЫ И ОПРЕДЕЛЕНИЯ

| Термин | Определение |
|--------|-------------|
| **Визит** | Совокупность работ, выполняемых инженером на одном объекте за одну смену |
| **Задача** | Единица обслуживания одной единицы оборудования в рамках визита |
| **Чек-лист** | Список задач на Странице 1 визита |
| **Фото ДО** | Фотография оборудования до начала работ |
| **Фото ПОСЛЕ** | Фотография оборудования после выполнения работ |
| **Сезон** | Период эксплуатации: «Лето» (01.04–31.10) или «Зима» (01.11–31.03) |
| **PWA** | Progressive Web App — технология для работы web-приложения офлайн |
| **152-ФЗ** | Федеральный закон «О персональных данных» |
| **СС** | Сплит-система |
| **МСС** | Мультисплит-система |
| **VRV** | Variable Refrigerant Volume — мультизональная система кондиционирования |
| **РЩ/ГРЩ** | Распределительный щит / Главный распределительный щит |
| **ХВС/ГВС** | Холодное / Горячее водоснабжение |
| **ТМ (Территориальный менеджер)** | Диспетчер/контролёр региона, управляющий визитами закреплённых инженеров |

---

## 3. РОЛЕВАЯ МОДЕЛЬ

### 3.1. Роли пользователей

| Роль | Интерфейс | Функции |
|------|-----------|---------|
| **Инженер** | Мобильное PWA (смартфон/планшет) | Создание визита, ведение чек-листа, заполнение параметров, фотофиксация, формирование и отправка отчета |
| **ТМ** | Web-интерфейс (десктоп/планшет) | Планирование работ, контроль визитов своих инженеров, переназначение, формирование и отправка отчётов, дашборд мониторинга |
| **Администратор** | Web-интерфейс (десктоп) | Ведение справочников: адреса, виды оборудования, типовые рекомендации, типы помещений, пользователи, email заказчиков |
| **Заказчик** | Email-клиент | Получение готового отчета в виде ZIP-архива (PDF + фотографии) |

### 3.2. Матрица доступа

| Функция | Инженер | ТМ | Администратор | Заказчик |
|---------|:-------:|:--:|:-------------:|:--------:|
| Создание визита (для себя) | ✅ | ❌ | ❌ | ❌ |
| Создание визита (назначение инженеру) | ❌ | ✅ | ✅ | ❌ |
| Заполнение параметров | ✅ | ❌ | ❌ | ❌ |
| Фотофиксация | ✅ | ❌ | ❌ | ❌ |
| Формирование отчёта | ✅ | ✅ | ✅ | ❌ |
| Отправка отчёта по email | ✅ | ✅ | ✅ | ❌ |
| Ведение справочников | ❌ | ❌ | ✅ | ❌ |
| Просмотр визитов своих | ✅ | ✅ | ✅ | ❌ |
| Просмотр визитов своих инженеров | ❌ | ✅ | ❌ | ❌ |
| Просмотр всех визитов | ❌ | ❌ | ✅ | ❌ |
| Переназначение визитов | ❌ | ✅ | ✅ | ❌ |
| Удаление визитов (soft delete) | ✅ | ✅ | ✅ | ❌ |
| Управление привязками ТМ | ❌ | ❌ | ✅ | ❌ |
| Управление привязкой Объект-Оборудование | ❌ | ❌ | ✅ | ❌ |
| Утверждение предложенного оборудования | ❌ | ❌ | ✅ | ❌ |
| Формирование сводного отчёта | ❌ | ✅ | ✅ | ❌ |
| Формирование отчёта по объекту | ❌ | ✅ | ✅ | ❌ |
| Получение отчёта по email | ❌ | ❌ | ❌ | ✅ |
| Управление специализацией | ✅ (самоуправление) | ✅ (подчинённые) | ✅ (подчинённые) | ❌ |
| Избранные объекты | ✅ | ✅ | ❌ | ❌ |
| Просмотр профиля | ✅ | ✅ | ✅ | ❌ |
| Импорт внешних заявок | ❌ | ✅ | ✅ | ❌ |
| Просмотр заявок (свои/территория/все) | ✅ (свои) | ✅ (территория) | ✅ (все) | ❌ |
| Назначение инженера на заявку | ❌ | ✅ | ✅ | ❌ |
| Отказ от заявки | ✅ | ❌ | ❌ | ❌ |
| Отчёт по номерам заявок | ❌ | ✅ | ✅ | ❌ |

---

## 4. ФУНКЦИОНАЛЬНЫЕ ТРЕБОВАНИЯ

### 4.1. Модуль «Инженер»

#### 4.1.1. Создание визита

**US-1.1 — Выбор адреса из справочника**  
*Как* инженер, *я хочу* выбирать адрес объекта из справочника с функцией поиска/автодополнения, *чтобы* избежать ошибок ввода и ускорить начало работы.

**US-1.2 — Автозаполнение даты и времени**  
*Как* инженер, *я хочу*, чтобы поля «Дата начала» и «Время начала» заполнялись автоматически текущими значениями, *чтобы* не вводить их вручную.

**US-1.3 — Автоопределение сезона**  
*Как* инженер, *я хочу*, чтобы поле «Сезон» автоматически определялось по дате визита:
- 01.04–31.10 → «Лето»
- 01.11–31.03 → «Зима»

*чтобы* исключить расхождения в отчетах.  
Поле доступно для ручного редактирования. При изменении даты — пересчет с подтверждением.

**US-1.4 — Добавление оборудования через модальное окно**
*Как* инженер, *я хочу* нажимать «+ Добавить оборудование» и выбирать нужные единицы оборудования из модального окна с тремя вкладками («Уровень помещения», «Уровень объекта» и «Добавить новое»), *чтобы* формировать состав визита пошагово, без дубликатов и визуального шума.
При первом нажатии кнопки для несохранённого визита система автоматически сохраняет визит как черновик, затем открывает модальное окно.

Вкладки модального окна:
- **«Уровень помещения»** — для оборудования с привязкой к помещению (климатическое оборудование: СС, МСС, VRV, тепловые завесы, приточные и приточно-вытяжные установки и т.д.). Инженер выбирает помещение из списка, затем видит всё оборудование в этом помещении (все чекбоксы отмечены по умолчанию) и нажимает «Добавить».
- **«Уровень объекта»** — для оборудования без привязки к помещению (сети, тепловые сети, приборы учёта, РЩ без помещения). Список с чекбоксами, все отмечены по умолчанию.
- **«Добавить новое»** — ручной ввод оборудования, которого нет в привязке объекта.

Уровень привязки определяется по наличию `roomTypeCode` в записи `object_equipment`, а не по коду типа оборудования.

**Создание нового помещения inline:**
Вкладка «Добавить новое» позволяет создать новое помещение прямо в модальном окне добавления оборудования (без перехода в справочник). При создании нового помещения отображается всё оборудование объекта (из всех помещений и с уровня объекта) с кнопкой «Перенести» для переноса выбранного оборудования в созданное помещение.

**US-1.5 — Визуальный статус задач**  
*Как* инженер, *я хочу* видеть в чек-листе для каждой задачи:
- Статус: «Не начато» / «В работе» / «Выполнено»
- Прогресс фото: ⬜ / 📷 1/2 / 📷 2/2

*чтобы* понимать, что уже сделано.

**US-1.6 — Возврат к незавершенной задаче**
*Как* инженер, *я хочу* иметь возможность кликнуть на любую незавершенную задачу и продолжить её заполнение, *чтобы* не начинать работу заново.

**US-1.7 — Работа офлайн**
*Как* инженер, *я хочу* создавать визиты и загружать фото без подключения к интернету, *чтобы* работать в подвалах, серверных и других зонах без связи.
Данные сохраняются локально и автоматически синхронизируются при восстановлении соединения.

**US-1.8 — Выбор оборудования из привязки объекта через модальное окно (группировка по уровням)**
*Как* инженер, *я хочу*, чтобы при нажатии «+ Добавить оборудование» оборудование из привязки объекта (`object_equipment`) отображалось сгруппированным по уровню привязки — с помещениями и без, *чтобы* выбирать нужные позиции осознанно, без дубликатов.

Система загружает список оборудования из `object_equipment` при открытии модального окна. Уже добавленные в визит единицы скрыты из списка. При повторном открытии модалки отображаются только оставшиеся. Если всё оборудование уже добавлено — соответствующая вкладка показывает сообщение «Всё оборудование уже добавлено в визит».

**Группировка по уровням привязки:**
- **Уровень помещения** — оборудование, у которого указан `roomTypeCode` в `object_equipment`. Инженер сначала выбирает помещение из выпадающего списка, затем видит все единицы оборудования в этом помещении. Все чекбоксы отмечены по умолчанию. После нажатия «Добавить» выбранные единицы добавляются в визит.
- **Уровень объекта** — оборудование без привязки к помещению (`roomTypeCode` отсутствует). Отображается плоским списком с чекбоксами, все отмечены по умолчанию.

**Уточнение помещения:**
Для единиц оборудования, у которых не указан тип помещения, отображается метка «⚠️ Помещение не указано». При выборе такого оборудования появляется inline-список для выбора типа помещения из справочника. После добавления визита выбранное помещение сохраняется в `object_equipment` (поля `room_confirmed_at`, `room_confirmed_by`) и используется во всех последующих визитах.

**Новый API-эндпоинт:**
`GET /api/refs/object-equipment/rooms?address_id=...&exclude_visit_id=...` — возвращает список помещений с количеством оборудования для текущего объекта (исключая уже добавленные в указанный визит).

**US-1.9 — Предложение добавить оборудование в привязку объекта**
*Как* инженер, *я хочу* отметить чекбоксом «Предложить добавить в привязку объекта» при добавлении оборудования, *чтобы* предложить администратору закрепить обнаруженное оборудование за данным объектом для будущих визитов.
Предложение сохраняется со статусом `pending` и становится доступным администратору для утверждения или отклонения.

**US-GROUP-01 — Группировка оборудования по уровню помещения**
*Как* инженер, *я хочу* выбирать оборудование с привязкой к помещению, предварительно выбирая нужное помещение из списка, *чтобы* быстро находить оборудование в конкретных помещениях объекта.
На вкладке «📍 Уровень помещения» инженер видит выпадающий список помещений (полученный через `GET /api/refs/object-equipment/rooms`). После выбора помещения отображается чекбокс-лист всех единиц оборудования в этом помещении. Все чекбоксы отмечены по умолчанию. Уровень привязки определяется по наличию `roomTypeCode` в записи `object_equipment`.

**US-GROUP-02 — Группировка оборудования по уровню объекта**
*Как* инженер, *я хочу* видеть оборудование без привязки к помещению отдельным списком, *чтобы* добавлять его в визит без необходимости указывать помещение.
На вкладке «🏠 Уровень объекта» отображается плоский чекбокс-лист единиц оборудования из `object_equipment`, у которых `roomTypeCode` отсутствует (сети, тепловые сети, приборы учёта, РЩ без помещения). Все чекбоксы отмечены по умолчанию.

**US-METER-01 — Автозаполнение полей счётчиков из справочника объекта**
*Как* инженер, *я хочу*, чтобы при открытии задачи-счётчика (Прибор учёта э/э, ХВС, ГВС), созданной из связанного оборудования (`object_equipment`), поля «Модель счётчика» и «Номер счётчика» автоматически заполнялись из данных привязки, *чтобы* не вводить повторяющиеся данные вручную.
Логика автозаполнения:
- «Модель счётчика» = `brand` + " " + `model` (конкатенация; если `brand` пуст — только `model`)
- «Номер счётчика» = `serial_number`
Поля остаются редактируемыми после автозаполнения. Рядом с автозаполненными полями отображается иконка 📋 с тултипом «Автозаполнено из справочника объекта».

#### 4.1.6. Группировка климатического оборудования в единый чек-лист

**US-GROUP-NEW-01 — Групповой чек-лист для климата в помещении**
*Как* инженер ВиК, *я хочу*, чтобы при добавлении климатического оборудования в помещении формировался один групповой чек-лист, *чтобы* не заполнять одинаковые параметры (температура помещения, заключение) для каждой единицы отдельно.
Система создаёт одну задачу типа `group_climate` на все внутренние блоки климатического оборудования (СС, МСС, VRV) в помещении. Общие параметры (температура, заключение, рекомендации) заполняются один раз. Для каждой единицы оборудования инженер указывает индивидуальный статус (ОК/Не ОК) и загружает фото ДО/ПОСЛЕ.

**US-GROUP-NEW-02 — Групповой чек-лист для наружных блоков**
*Как* инженер, *я хочу*, чтобы наружные блоки кондиционеров (splitnar, mssnar, vrv_nar) группировались в отдельную задачу по уровню объекта (здания), *чтобы* не привязывать их к конкретному помещению.
Наружные блоки определяются по полю `is_outdoor_unit = true` в `object_equipment`. Все наружные блоки объекта формируют одну групповую задачу.

**US-GROUP-NEW-03 — Индивидуальные статусы и фото для каждой единицы**
*Как* инженер, *я хочу* для каждой единицы в групповом чек-листе указывать индивидуальный статус (ОК/Не ОК) и загружать 2 фото (до/после), *чтобы* фиксировать состояние каждого кондиционера отдельно.

**US-GROUP-NEW-04 — Добавление и удаление единиц в групповой задаче**
*Как* инженер, *я хочу* иметь возможность добавлять и удалять единицы оборудования в групповом чек-листе, *чтобы* гибко формировать состав работ.

**US-GROUP-NEW-05 — Валидация и сохранение групповой задачи**
*Как* система, *я хочу* разрешать сохранение групповой задачи с частичным заполнением, *чтобы* инженер мог сохранить прогресс и вернуться к незавершённым единицам позже.
Логика сохранения:
- Инженер может нажать «Сохранить» в любой момент — общие параметры и заполненные данные единиц сохраняются
- Статус задачи определяется автоматически:
  - `in_progress` («В работе») — если хотя бы одна единица не имеет статуса или фото
  - `completed` («Выполнено») — если все единицы имеют статус и оба фото (ДО и ПОСЛЕ)
- При сохранении неполной задачи отображается сообщение: «Сохранено. Не завершено единиц: N»
- Если хотя бы одна единица имеет статус «Не ОК», поле «Дополнительные рекомендации» становится обязательным

**US-GROUP-NEW-06 — Единообразие структуры при одной единице**
*Как* система, *я хочу* даже при наличии только одной единицы климатического оборудования в помещении формировать групповую задачу, *чтобы* обеспечить единообразие структуры данных.

**Принцип группировки задач:**

| Тип задачи | Оборудование | Уровень | Формирование |
|------------|-------------|---------|--------------|
| **Групповая (климат)** | Внутренние блоки СС, МСС, VRV | Помещение | Один чек-лист на все единицы в помещении |
| **Групповая (наружные)** | Наружные блоки СС, МСС, VRV | Объект | Один чек-лист на все наружные блоки объекта |
| **Индивидуальная** | Счётчики, РЩ/ГРЩ, сети, вентиляция | По единице | Отдельный чек-лист на каждую единицу |

#### 4.1.2. Заполнение параметров

**US-2.1 — Быстрый ввод рекомендаций**  
*Как* инженер, *я хочу* отмечать галочками типовые рекомендации из справочника (с фильтрацией по виду оборудования), *чтобы* не печатать текст на телефоне в полевых условиях.

**US-2.2 — Условная обязательность**  
*Как* система, *я хочу* делать поле «Дополнительные рекомендации» обязательным только при выборе заключения «Исправно, есть замечания» или «Неисправно», *чтобы* инженер описывал проблему, но не тратил время, если всё в норме.

**US-2.3 — Безопасное редактирование**  
*Как* инженер, *я хочу*, чтобы кнопка «Сбросить» очищала **только текущую задачу**, а не весь визит, *чтобы* случайное нажатие не уничтожило уже проделанную работу.

#### 4.1.3. Фотофиксация

**US-3.1 — Фото ДО и ПОСЛЕ за один проход**  
*Как* инженер, *я хочу* на Странице 5 сразу загружать оба фото (до и после работ), *чтобы* зафиксировать результат без возврата в чек-лист.

**US-3.2 — Разное количество фото**
*Как* система, *я хочу* требовать:
- 2 фото (ДО/ПОСЛЕ) — для климатического оборудования (СС, МСС, VRV, тепловая завеса, приточная установка, приточно-вытяжная установка, вытяжная установка)
- 1 фото (ДО) — для остального оборудования (РЩ, счетчики, сети)

*чтобы* логика соответствовала технологическим требованиям.

**US-3.3 — Защита персональных данных (152-ФЗ)**  
*Как* система, *я хочу* показывать предупреждение при первом фото в клиентских зонах:
```
⚠️ ВНИМАНИЕ!
Убедитесь, что в кадр не попали:
• Лица посетителей
• Персональные данные на мониторах
• Банковские карты и документы
[Понятно, сделать фото]
```

#### 4.1.4. Завершение и отчет

**US-4.1 — Формирование отчета**  
*Как* инженер, *я хочу* по кнопке «Завершить визит» получить PDF-отчет и ZIP-архив с фото, *чтобы* отправить результат заказчику.

**US-4.2 — Маркировка фотографий**  
*Как* система, *я хочу* автоматически именовать файлы по шаблону:
```
{№}_{тип_оборудования}_{помещение}_{moment}.jpg
```
Примеры:
- `01_rsch_server_before.jpg`
- `02_splitvn_server_before.jpg`
- `02_splitvn_server_after.jpg`

**US-4.3 — Отправка по email**  
*Как* инженер, *я хочу* после формирования отчета отправить ZIP-архив на email заказчика (предзаполненный из справочника), *чтобы* не скачивать файл и не пересылать вручную.

**US-4.4 — Резервное скачивание**  
*Как* инженер, *я хочу* иметь возможность скачать ZIP-архив на устройство, *чтобы* переслать его вручную при недоступности email.

#### 4.1.5. Работа в полевых условиях

**US-5.1 — Офлайн-режим**  
*Как* инженер, *я хочу*, чтобы приложение работало без интернета (подвалы, серверные, тепловые узлы), *чтобы* не прерывать работу из-за отсутствия связи.

**US-5.2 — Автосинхронизация**  
*Как* система, *я хочу* при появлении сети автоматически отправлять накопленные данные и фото, *чтобы* отчет был доступен без моих действий.

**US-5.3 — Автосохранение**
*Как* система, *я хочу* автоматически сохранять данные каждые 30 секунд и предупреждать при закрытии с несохраненными данными.
Реализовано: автосохранение срабатывает по таймеру (интервал 30 секунд), при переключении между задачами и перед формированием отчёта. В интерфейсе отображается визуальный индикатор состояния: «Сохранено» / «Сохранение...» / «Не сохранено». При попытке закрыть страницу с несохранёнными данными — предупреждение `beforeunload`.

**US-5.4 — Автообновление приложения**
*Как* система, *я хочу* при деплое новой версии автоматически предлагать пользователю обновить приложение, *чтобы* пользователь всегда работал с актуальной версией без необходимости вручную очищать кэш.
При обнаружении новой версии внизу экрана появляется баннер «Доступна новая версия приложения» с кнопками «Обновить» и «Закрыть». Нажатие «Обновить» перезагружает страницу с новой версией.

**US-5.5 — Автообновление данных при возврате**
*Как* система, *я хочу* при возврате пользователя на вкладку автоматически обновлять данные (визиты, справочники), *чтобы* пользователь видел актуальную информацию без ручного обновления страницы.
Синхронизация данных запускается при событии `visibilitychange` (переход на вкладку). Оборудование в модальном окне загружается с сервера без кэширования при каждом открытии.

### 4.2. Модуль «Администратор»

**US-ADM-1 — CRUD справочника адресов**
*Как* администратор, *я хочу* добавлять, редактировать и удалять адреса (город, улица, дом, строение, полный адрес), *чтобы* инженер выбирал из списка.
Удаление адресов выполняется как **soft delete** (пометка `is_deleted`) — адрес скрывается из списков и поиска, но сохраняется для исторических визитов и связанных записей.

**US-ADM-2 — CRUD справочника рекомендаций**
*Как* администратор, *я хочу* вести справочник типовых рекомендаций с привязкой к виду оборудования (38 активных категорий + 1 деактивированная), *чтобы* инженер выбирал их чек-боксами.

**US-ADM-3 — CRUD видов оборудования**
*Как* администратор, *я хочу* управлять списком видов оборудования (39 записей: 38 активных + 1 деактивированный) и привязанными контролируемыми параметрами, *чтобы* гибко настраивать приложение.
Каждый вид оборудования имеет поле `specialization_req` (TEXT, nullable) — требуемая специализация (`vik`, `iszh`, `gpm`, `dgu` или `ibp`), определяющее, для какой специализации инженера доступно данное оборудование.

**US-ADM-4 — CRUD типов помещений**  
*Как* администратор, *я хочу* управлять списком типов помещений (Электрощитовая, Серверная, Клиентский зал и т.д.).

**US-ADM-5 — Привязка email к адресу**  
*Как* администратор, *я хочу* указывать email заказчика при создании/редактировании объекта, *чтобы* инженер мог сразу отправить отчет.

**US-ADM-6 — Управление пользователями**
*Как* администратор, *я хочу* создавать, редактировать и блокировать учетные записи инженеров с привязкой ФИО, управлять специализацией и искать пользователей по ФИО/email.
При создании/редактировании инженера администратор выбирает специализацию (ВиК, ИСЖ, ГПМ, ДГУ, ИБП) через чек-боксы. По умолчанию выбрана ИСЖ. Валидация: хотя бы одна специализация обязательна. При смене роли на ТМ/Админ специализация сбрасывается.
В таблице пользователей отображается колонка «Спец.» с текущей специализацией инженера. Над таблицей — строка поиска по ФИО и email (case-insensitive, частичное совпадение, debounce 300ms, лимит 50 записей).

**US-ADM-7 — Массовый импорт справочников**
*Как* администратор, *я хочу* загружать справочники (адреса, оборудование, рекомендации, пользователи) из файлов форматов CSV и XLSX, *чтобы* быстро наполнять систему начальными данными без ручного ввода каждой записи.
Поддерживаемые возможности:
- Поддержка форматов: `.csv` (разделители `,` и `;`, кодировки UTF-8 / Windows-1251) и `.xlsx` (Microsoft Excel 2007+)
- Для Excel-файлов: данные считываются с первого листа, первая строка — заголовки столбцов, пустые строки игнорируются
- Предварительная валидация (pre-validation) — проверка формата и обязательных полей до импорта с отображением списка ошибок
- Автоопределение кодировки файла (UTF-8 / Windows-1251) для CSV
- Ограничение на количество строк: до 20 000 за один импорт
- Ограничение на размер файла: до 10 МБ
- Для импорта пользователей — автоматическая генерация временных паролей и отправка их на email пользователю с уведомлением об учётной записи
- Rate limiting на эндпоинтах аутентификации: не более 20 запросов за 15 минут для защиты от брутфорса

**US-ADM-8 — Управление привязкой «Объект-Оборудование»**
*Как* администратор, *я хочу* управлять привязкой оборудования к объектам (адресам): добавлять, редактировать, деактивировать записи, *чтобы* инженер при создании визита выбирал оборудование для известных объектов.
Интерфейс позволяет:
- Выбрать адрес из справочника и увидеть список привязанного оборудования
- Добавить новую единицу оборудования с указанием марки, модели, серийного номера, типа помещения (опционально) и описания расположения
- Деактивировать оборудование (без физического удаления — для сохранения истории визитов)
- Записи без указанного помещения отображаются с пометкой «⚠️ Не указано»
- При импорте оборудования поле «Тип помещения» является опциональным — оборудование может быть привязано к объекту без указания конкретного помещения
- При редактировании оборудования в поле «Объект» отображается код и адрес объекта, а не UUID
- При добавлении оборудования отображается список существующего оборудования объекта с кнопкой «Перенести» для переноса оборудования между объектами

**US-ADM-9 — Утверждение предложенного оборудования**
*Как* администратор, *я хочу* просматривать и утверждать/отклонять предложения инженеров о добавлении оборудования в привязку объекта, *чтобы* контролировать состав привязанного оборудования и учитывать полевые наблюдения инженеров.
Интерфейс показывает список предложений со статусом `pending`: инженер, адрес, предложенное оборудование. Администратор может утвердить (оборудование добавляется в `object_equipment` со статусом `is_active = true`) или отклонить (статус предложения → `rejected`) с комментарием.

### 4.3. Модуль «Территориальный менеджер (ТМ)»

**US-TM-1 — Просмотр визитов своих инженеров**
*Как* ТМ, *я хочу* видеть все визиты закреплённых за мной инженеров, *чтобы* контролировать ход выполнения работ.
Список визитов отображается с пагинацией (20 визитов на странице) с навигацией по страницам и отображением «1–20 из N». При изменении фильтров страница сбрасывается на первую.
Доступные фильтры: поиск по адресу или коду объекта (с debounce 400ms), мульти-выбор статусов, фильтр по инженеру.
Свитчер «Активные/Удалённые» доступен только администратору.

**US-TM-2 — Дашборд мониторинга**
*Как* ТМ, *я хочу* видеть сводную статистику (запланированные, в работе, завершённые, отправленные), *чтобы* оценивать загрузку и статус работ.
Статистика рассчитывается по **всем** визитам (без учёта пагинации), чтобы цифры отражали реальную картину по всем закреплённым инженерам.

**US-TM-3 — Фильтрация визитов по инженеру**
*Как* ТМ, *я хочу* фильтровать визиты по конкретному инженеру, *чтобы* быстро находить нужные данные.

**US-TM-4 — Переназначение визита**
*Как* ТМ, *я хочу* переназначить незавершённый визит с одного инженера на другого из своей группы, *чтобы* обеспечить выполнение работ при замене.

**US-TM-5 — Редактирование визита инженера**
*Как* ТМ, *я хочу* редактировать данные визита перед отправкой, *чтобы* скорректировать отчёт.

**US-TM-6 — Формирование и отправка отчёта**
*Как* ТМ, *я хочу* формировать и отправлять отчёт по визиту своего инженера, *чтобы* обеспечить резервирование процесса отправки.

**US-TM-7 — Удаление визита**
*Как* ТМ, *я хочу* удалять визиты своих инженеров (soft delete), *чтобы* исключать некорректные данные из отчётности.

**US-TM-8 — Формирование сводного отчёта за период**
*Как* ТМ, *я хочу* формировать сводный отчёт по всем визитам за указанный период (дата начала — дата окончания), *чтобы* предоставлять руководству агрегированную информацию о выполненных работах.
Отчёт имеет единую структуру: титульный лист (название, период, дата формирования, ФИО сформировавшего), группировка по объектам (каждый с новой страницы), визиты с задачами, параметры, превью фотографий (width ≤ 400px), заключения, рекомендации. Формат — PDF.

**US-TM-9 — Формирование отчёта по объектам**
*Как* ТМ, *я хочу* выбирать один или несколько объектов и формировать отчёт за выбранный период, *чтобы* консолидировать данные по группе зданий.
Отчёт имеет идентичную структуру с отчётом за период (US-TM-8). Поддерживается мультивыбор объектов через чек-боксы. Период обязателен.

**US-TM-12 — Превью фотографий в сводном отчёте**
*Как* ТМ, *я хочу*, чтобы в сводном PDF-отчёте были встроены визуальные превью фотографий, *чтобы* оценивать качество работ без открытия ZIP-архива.
Фотографии масштабируются через `sharp` до width ≤ 400px и встраиваются в PDF как base64. При превышении 200 фото — автоматическое переключение в упрощённый режим (текстовые метки + отдельный ZIP).

**US-TM-13 — Загрузка сканов подписанных актов**
*Как* ТМ, *я хочу* прикреплять сканы подписанных актов (JPG/PNG/PDF, до 10 файлов, до 50 МБ) при формировании сводного отчёта, *чтобы* они автоматически включались в итоговый PDF.
Изображения автоматически сжимаются на сервере (width ≤ 1200px, JPEG quality 80%, EXIF удалён). PDF-файлы объединяются через `pdf-lib`. Загрузка необязательна.

**US-TM-10 — Закреплённые объекты в профиле ТМ**
*Как* ТМ, *я хочу* видеть в своём профиле список закреплённых за мной объектов (адресов), *чтобы* быстро переходить к созданию визитов на этих объектах.
Для каждого объекта отображается адрес, аналитический код и индикатор наличия неисправностей из последнего визита. Клик по объекту открывает форму создания визита.
Список содержит пагинацию (по умолчанию 10 объектов на странице, выбор 10/20/50) с отображением «1–10 из N» и счётчиком общего количества объектов в заголовке карточки.

**US-TM-11 — Создание инженера с привязкой к ТМ**
*Как* ТМ, *я хочу* создавать учётные записи инженеров прямо из своего профиля с автоматической привязкой ко мне, *чтобы* быстро пополнять команду без обращения к администратору.
При создании указываются ФИО, email и специализация. Инженер получает временный пароль `Welcome2026!` на email с инструкцией сменить пароль при первом входе.

### 4.4. Модуль «Заказчик»

**US-ZAK-1 — Получение отчета**
*Как* заказчик, *я хочу* получать email с ZIP-архивом (PDF + маркированные фото), *чтобы* иметь доказательную базу выполненных работ.

### 4.5. Модуль «Специализация»

**US-SPEC-01 — Инженер выбирает специализацию в ЛК**
*Как* инженер, *я хочу* выбирать свою специализацию (ВиК, ИСЖ, ГПМ, ДГУ, ИБП) на странице профиля в личном кабинете, *чтобы* система фильтровала задачи и оборудование по моему профилю.
Инженер может выбрать любую комбинацию из 5 доступных специализаций. Изменения вступают в силу немедленно.

**US-SPEC-02 — ТМ изменяет специализацию подчинённого инженера**
*Как* ТМ (или администратор), *я хочу* изменять специализацию закреплённого за мной инженера, *чтобы* управлять распределением задач в группе.
ТМ видит список своих инженеров с текущими специализациями и может переключать чекбоксы ВиК/ИСЖ/ГПМ/ДГУ/ИБП.

**US-SPEC-03 — Специализация по умолчанию при создании инженера**
*Как* система, *я хочу* при создании нового инженера устанавливать специализацию ИСЖ по умолчанию (остальные — false), *чтобы* инженер сразу мог работать с базовым оборудованием без дополнительной настройки.
Значения по умолчанию: `specialization_iszh = true`, остальные `specialization_*` = false.

**US-SPEC-04 — Блокирующий экран при отсутствии специализации**
*Как* система, *я хочу* показывать блокирующий экран (SpecializationGate), если у инженера не выбрана ни одна специализация, *чтобы* предотвратить создание визитов без профильной специализации.
Экран содержит 5 чекбоксов выбора специализации (ВиК, ИСЖ, ГПМ, ДГУ, ИБП) и кнопку «Сохранить». Доступ к основному интерфейсу заблокирован до выбора хотя бы одной специализации.

**Маппинг специализаций и оборудования:**

| Специализация | Коды оборудования |
|:-------------:|-------------------|
| **ВиК** (Вентиляция и кондиционирование) | ~~vent~~ (деактивирован), teplozavesa, pritochnaya, pritochno-vytyzhnaya, vytyzhnaya, vrv_vn, vrv_nar, mssvn, mssnar, splitvn, splitnar, cond_mobile |
| **ИСЖ** (Инженерные сети и здания) | rsch, schetchik_gvs, schetchik_hvs, schetchik_electroshc, seti_vodosnab, teplovye_seti, itp, boiler_gas, boiler_liquid, boiler_solid, boiler_elec, meter_gas, sololift, barrier_roller, door_auto, coffee, purifier, cooler, aquarium, bubble_panel |
| **ГПМ** (Грузоподъёмные механизмы) | lift_pass, lift_cargo, lift_cargo_pass, lift_invalid |
| **ДГУ** (Дизель-генераторные установки) | dgu, mkgu |
| **ИБП** (Источники бесперебойного питания) | ibp |

> **Примечание:** Вид оборудования `vent` («Вентиляционная установка») деактивирован и скрыт из интерфейса. Вместо него используются 4 подвида: `teplozavesa` (Тепловая завеса), `pritochnaya` (Приточная установка), `pritochno-vytyzhnaya` (Приточно-вытяжная установка), `vytyzhnaya` (Вытяжная установка). Запись `vent` сохранена в базе данных для обратной совместимости с ранее созданными задачами. Все 4 новых вида используют ту же форму параметров, что и старый `vent` (Страница 4.3), и относятся к специализации ВиК.

**Новое поле `specialization_req` в таблице `equipment_types`:**

| Поле | Тип | Описание |
|------|-----|----------|
| specialization_req | TEXT (nullable) | Требуемая специализация: `vik` (ВиК), `iszh` (ИСЖ), `gpm` (ГПМ), `dgu` (ДГУ) или `ibp` (ИБП). Если NULL — оборудование доступно всем специализациям |

### 4.6. Модуль «Личный кабинет»

**US-LK-01 — Инженер добавляет объекты в Избранное**
*Как* инженер (или ТМ), *я хочу* добавлять объекты в список избранных (⭐), *чтобы* быстро находить часто посещаемые объекты.
Избранные объекты отображаются на странице профиля с возможностью быстрого перехода. Максимум 20 объектов.

**US-LK-02 — Избранное доступно офлайн**
*Как* инженер, *я хочу*, чтобы список избранных объектов был доступен без интернета (синхронизация с IndexedDB), *чтобы* пользоваться избранным в полевых условиях.

**US-LK-03 — Предупреждение при отсутствии профильного оборудования**
*Как* система, *я хочу* показывать предупреждение, если на избранном объекте нет оборудования специализации инженера, *чтобы* инженер заранее знал, что визит может быть непродуктивным.

**US-LK-04 — ТМ видит красный индикатор при неисправностях на закреплённых объектах**
*Как* ТМ, *я хочу* видеть красный индикатор (⚠️) рядом с закреплёнными за мной объектами в профиле, на которых есть неисправности в последних визитах, *чтобы* оперативно реагировать на проблемы.

**US-LK-05 — Админ видит системную информацию в ЛК**
*Как* администратор, *я хочу* видеть в личном кабинете быстрые ссылки на справочники и системную информацию, *чтобы* быстро навигировать по административным функциям.

---

## 5. ОПИСАНИЕ ИНТЕРФЕЙСОВ

### 5.1. Страница 1: «Визит» (Чек-лист задач)

**Поля:**
- 1.1. `Адрес объекта` — поиск по справочнику (обязательно)
- 1.2. `Дата начала` — дата (автозаполнение, обязательно)
- 1.3. `Время начала` — время (автозаполнение, обязательно)
- 1.4. `Сезон` — «Лето» / «Зима» (автоопределение по дате, доступно для редактирования)

**Таблица «Проведённые работы»:**

| Чекбокс | Вид оборудования | Помещение | Марка / Модель | Вложения | Статус |
|:-------:|------------------|-----------|----------------|:--------:|--------|
| ☐ | Климатическое оборудование | Серверная | 3 единицы | 📷 6/6 | Выполнено |
| ☐ | Климатическое оборудование | Клиентский зал | 2 единицы | 📷 3/4 | В работе |
| ☐ | Наружные блоки кондиционеров | Уровень объекта | 2 единицы | 📷 4/4 | Выполнено |
| ☐ | Прибор учета э/э | Электрощитовая | Меркурий 230 | 📷 1/1 | Выполнено |
| ☐ | РЩ/ГРЩ | Электрощитовая | ABB GS420 | ⬜ | Не начато |

> **Примечание:** Климатическое оборудование (внутренние блоки СС, МСС, VRV) группируется в одну задачу на помещение. Наружные блоки группируются в одну задачу на объект. Остальное оборудование — индивидуальные задачи.

**Кнопки:**
- `+ Добавить оборудование` — открывает модальное окно «Добавление оборудования» с тремя вкладками:
  - **Вкладка «📍 Уровень помещения»** — для оборудования с привязкой к помещению (определено по наличию `roomTypeCode` в `object_equipment`). Инженер выбирает помещение из списка (полученного через `GET /api/refs/object-equipment/rooms`), затем видит всё оборудование в этом помещении в виде чекбокс-листа. Все чекбоксы отмечены по умолчанию. Уже добавленные в визит единицы скрыты. Инженер нажимает «Добавить» — для каждой выбранной записи создаётся задача с автоматическим заполнением полей (тип оборудования, помещение, марка, модель, серийный номер, расположение).
  - **Вкладка «🏠 Уровень объекта»** — для оборудования без привязки к помещению (`roomTypeCode` отсутствует). Отображается чекбокс-лист единиц оборудования из `object_equipment`, отфильтрованных по специализации инженера. Все чекбоксы отмечены по умолчанию. Уже добавленные в визит единицы скрыты.
  - **Вкладка «➕ Добавить новое»** — форма для добавления оборудования, которого нет в привязке объекта:
    - Вид оборудования (выпадающий список из справочника)
    - Тип помещения (выпадающий список из справочника)
    - Комментарий (текстовое поле, необязательно)
    - Марка (текстовое поле, необязательно)
    - Модель (текстовое поле, необязательно)
    - Серийный номер (текстовое поле, необязательно)
    - Чекбокс «Предложить добавить в привязку объекта» (если отмечен — при сохранении создаётся предложение со статусом `pending` для утверждения администратором)
- `Завершить визит` — формирует PDF + ZIP (активна при наличии хотя бы 1 выполненной задачи)

**Логика:**
- При открытии нового визита таблица «Проведённые работы» **полностью пустая**. Никакое оборудование автоматически не подтягивается.
- Кнопка `+ Добавить оборудование` доступна сразу. Для нового (несохранённого) визита — при нажатии визит автоматически сохраняется как черновик, затем открывается модальное окно.
- При повторном нажатии «+ Добавить оборудование» во вкладках «Уровень помещения» и «Уровень объекта» отображаются только те единицы, которые инженер ещё не добавил в текущий визит.
- Если всё оборудование определённого уровня уже добавлено, соответствующая вкладка показывает сообщение «Всё оборудование уже добавлено в визит».
- Клик на строку таблицы открывает соответствующую страницу параметров (4.x)
- Кнопка «Завершить визит» неактивна, пока хотя бы одна задача не выполнена

### 5.2. Страницы 4.x: «Контролируемые параметры»

> **Примечание:** Страница 2 «Объект контроля» и Страница 3 «Место установки» из исходного описания упразднены. Поля «Вид оборудования» и «Тип помещения» заполняются в модальном окне на Странице 1.

#### 5.2.0. Страница групповой задачи — Климатическое оборудование

Открывается при клике на строку «Климатическое оборудование» в чек-листе визита. Содержит **общие параметры** (один набор на все единицы) и **список единиц оборудования** с индивидуальными статусами и фото.

**Общие параметры:**

| № | Поле | Тип | Значения | По умолчанию | Обязательное |
|---|------|-----|----------|:------------:|:------------:|
| ГП-1 | Температура помещения на уровне 1,2м от пола | Number | °C | — | ✅ |
| ГП-2 | Заключение | Select | Исправно, замечаний нет / Исправно, есть замечания / Неисправно | Исправно, замечаний нет | ✅ |
| ГП-3 | Типовые рекомендации | Multi-checkbox | Из справочника (объединённый список по всем видам климата в задаче) | — | — |
| ГП-4 | Дополнительные рекомендации | Text | Свободный текст | — | Условно (обязательно при замечаниях или статусе «Не ОК» хотя бы одной единицы) |

**Список единиц оборудования:**

Для каждой единицы отображаются:
- Вид оборудования, марка, модель, серийный номер (из `object_equipment`)
- **Статус:** ✅ ОК / ⚠️ Не ОК (переключатель)
- **Фото ДО:** кнопка «📷 Сделать» / «🖼 Выбрать» + индикатор загрузки [x/2]
- **Фото ПОСЛЕ:** кнопка «📷 Сделать» / «🖼 Выбрать» + индикатор загрузки [x/2]

**Валидация:**
- Кнопка «Сохранить» активна всегда (после заполнения обязательных общих полей: температура, заключение)
- При сохранении система автоматически определяет статус задачи:
  - Все единицы имеют статус + фото → «Выполнено»
  - Хотя бы одна единица не заполнена → «В работе» (с сообщением «Не завершено единиц: N»)
- Инженер может сохранить неполную задачу и вернуться к ней позже
- При статусе «Не ОК» хотя бы одной единицы — поле «Дополнительные рекомендации» становится обязательным

**Защита от дубликатов фото:**
- Система запрещает загрузку одного и того же фото (определяется по размеру файла) для разных единиц оборудования в одной групповой задаче
- При попытке загрузить дубликат — ошибка «Это фото уже загружено для другой единицы оборудования»

**Кнопки:** `💾 Сохранить и вернуться в чек-лист` / `← Назад` / `🗑 Сбросить задачу`

> **Примечание:** Для групповой задачи наружных блоков структура аналогична, но вместо «Температура помещения» заполняются параметры наружных блоков: «Работоспособность», «Наличие утечек на трассах», «Наличие посторонних шумов».

#### 5.2.1. Страница 4.1 — РЩ/ГРЩ

| № | Поле | Тип | Значения | По умолчанию |
|---|------|-----|----------|:------------:|
| 4.1.1 | Состояние контактных соединений | Select | Удовлетворительно / Неудовлетворительно | Удовлетворительно |
| 4.1.2 | Состояние нулевого проводника | Select | Удовлетворительно / Неудовлетворительно | Удовлетворительно |
| 4.1.3 | Состояние заземляющего контура | Select | Удовлетворительно / Неудовлетворительно | Удовлетворительно |
| 4.1.4 | Состояние проводов (оплавление, подгоревшая изоляция) | Select | Удовлетворительно / Неудовлетворительно | Удовлетворительно |
| 4.1.5 | Исправность всех запирающих устройств | Select | Да / Нет | Да |
| 4.1.6 | Исправность работы аварийного освещения | Select | Да / Нет | Да |
| 4.1.7 | Исправность работы УЗО, автоматов диф.защиты | Select | Да / Нет | Да |
| 4.1.8 | Заключение | Select | Исправно, замечаний нет / Исправно, есть замечания / Неисправно | Исправно, замечаний нет |
| 4.1.9 | Типовые рекомендации | Multi-checkbox | Из справочника (категория «РЩ/ГРЩ») | — |
| 4.1.10 | Дополнительные рекомендации | Text | Свободный текст (обязательно при замечаниях) | — |

**Кнопки:** `Сохранить и вернуться в чек-лист` / `Назад` / `Сбросить задачу`

#### 5.2.2. Страница 4.2 — Прибор учета э/э

| № | Поле | Тип | Значения | По умолчанию | Обязательное |
|---|------|-----|----------|:------------:|:------------:|
| 4.2.1 | Модель счётчика | Text | — | — | ✅ |
| 4.2.2 | Номер счётчика | Text | — | — | ✅ |
| 4.2.3 | Трансформатор тока | Select | Да / Нет | Нет | — |
| 4.2.4 | Наличие пломбы | Select | Да / Нет | Да | — |
| 4.2.5 | Показания | Number | — | — | ✅ |
| 4.2.6 | Заключение | Select | Исправно, замечаний нет / Исправно, есть замечания / Неисправно | Исправно, замечаний нет | — |
| 4.2.7 | Типовые рекомендации | Multi-checkbox | Из справочника (категория «Прибор учета электроэнергии») | — | — |
| 4.2.8 | Дополнительные рекомендации | Text | — | — | Условно |

#### 5.2.3. Страница 4.3 — Вентиляционная установка

> **Примечание:** Данная форма параметров используется для видов оборудования: `vent` (деактивирован), `teplozavesa` (Тепловая завеса), `pritochnaya` (Приточная установка), `pritochno-vytyzhnaya` (Приточно-вытяжная установка), `vytyzhnaya` (Вытяжная установка). Все 4 новых подвида используют идентичную форму.

| № | Поле | Тип | Значения | По умолчанию | Обязательное |
|---|------|-----|----------|:------------:|:------------:|
| 4.3.1 | Наличие вентиляционной установки | Select | Да / Нет | Да | — |
| 4.3.2 | Режим работы | Select | Включена / Выключена | Включена | — |
| 4.3.3 | Наличие аварий и ошибок контроллера | Select | Да / Нет | Нет | — |
| 4.3.4 | Наличие посторонних шумов | Select | Да / Нет | Нет | — |
| 4.3.5 | Состояние воздушного фильтра | Select | Чистый / Требует замены | Чистый | — |
| 4.3.6 | Температура воздуха до теплообменника | Number | °C | — | ✅ |
| 4.3.7 | Температура воздуха после теплообменника | Number | °C | — | ✅ |
| 4.3.8 | Заключение | Select | Исправно, замечаний нет / Исправно, есть замечания / Неисправно | Исправно, замечаний нет | — |
| 4.3.9 | Типовые рекомендации | Multi-checkbox | Из справочника (категория «Вентиляционная установка») | — | — |
| 4.3.10 | Дополнительные рекомендации | Text | — | — | Условно |

#### 5.2.4. Страница 4.4 — Внутренний блок СС

| № | Поле | Тип | Значения | По умолчанию | Обязательное |
|---|------|-----|----------|:------------:|:------------:|
| 4.4.1 | Работоспособность | Select | Удовлетворительно / Неудовлетворительно | Удовлетворительно | — |
| 4.4.2 | Наличие посторонних шумов | Select | Да / Нет | Нет | — |
| 4.4.3 | Температура помещения на уровне 1,2м от пола | Number | °C | — | ✅ |
| 4.4.4 | Состояние фильтра | Select | Чистый / Необходима очистка | Необходима очистка | — |
| 4.4.5 | Необходимость внеплановой промывки дренажной системы | Select | Да / Нет | Нет | — |
| 4.4.6 | Наличие утечек хладагента | Select | Да / Нет | Нет | — |
| 4.4.7 | Холодопроизводительность, кВт | Number | — | — | ✅ |
| 4.4.8 | Заключение | Select | Исправно, замечаний нет / Исправно, есть замечания / Неисправно | Исправно, замечаний нет | — |
| 4.4.9 | Типовые рекомендации | Multi-checkbox | Из справочника (категория «Внутренний блок сплит-системы») | — | — |
| 4.4.10 | Дополнительные рекомендации | Text | — | — | Условно |

#### 5.2.5. Страница 4.5 — Наружный блок СС

| № | Поле | Тип | Значения | По умолчанию |
|---|------|-----|----------|:------------:|
| 4.5.1 | Работоспособность | Select | Удовлетворительно / Неудовлетворительно | Удовлетворительно |
| 4.5.2 | Наличие утечек на трассах | Select | Да / Нет | Нет |
| 4.5.3 | Наличие посторонних шумов кондиционеров | Select | Да / Нет | Нет |
| 4.5.4 | Необходимость внеплановой промывки наружного блока | Select | Да / Нет | Нет |
| 4.5.5 | Заключение | Select | Исправно, замечаний нет / Исправно, есть замечания / Неисправно | Исправно, замечаний нет |
| 4.5.6 | Типовые рекомендации | Multi-checkbox | Из справочника (категория «Наружный блок сплит-системы») | — |
| 4.5.7 | Дополнительные рекомендации | Text | — | — |

#### 5.2.6. Страница 4.6 — Внутренний блок МСС

Аналогично 4.4 с теми же полями. Справочник рекомендаций — категория «Внутренний блок мультисплит системы».

#### 5.2.7. Страница 4.7 — Наружный блок МСС

Аналогично 4.5. Справочник рекомендаций — категория «Наружный блок мультисплит системы».

#### 5.2.8. Страница 4.8 — Внутренний блок VRV

Аналогично 4.4. Справочник рекомендаций — категория «Внутренний блок VRV».

#### 5.2.9. Страница 4.9 — Наружный блок VRV

Аналогично 4.5. Справочник рекомендаций — категория «Наружный блок VRV».

#### 5.2.10. Страница 4.10 — Сети водоснабжения и водоотведения

| № | Поле | Тип | Значения | По умолчанию |
|---|------|-----|----------|:------------:|
| 4.10.1 | Наличие повреждений трубопровода ХВС | Select | Да / Нет | Нет |
| 4.10.2 | Наличие повреждений трубопровода ГВС | Select | Да / Нет | Нет |
| 4.10.3 | Наличие коррозии на трубопроводах ХВС | Select | Да / Нет | Нет |
| 4.10.4 | Наличие коррозии на трубопроводах ГВС | Select | Да / Нет | Нет |
| 4.10.5 | Наличие свищей/протечек на трубопроводах ХВС | Select | Да / Нет | Нет |
| 4.10.6 | Наличие свищей/протечек на трубопроводах ГВС | Select | Да / Нет | Нет |
| 4.10.7 | Наличие неисправностей крепежей | Select | Да / Нет | Нет |
| 4.10.8 | Наличие прочих неисправностей трубопроводов ХВС | Select | Да / Нет | Нет |
| 4.10.9 | Наличие прочих неисправностей трубопроводов ГВС | Select | Да / Нет | Нет |
| 4.10.10 | Герметичность запорной, защитной и регулирующей арматуры | Select | Да / Нет | Нет |
| 4.10.11 | Сливные воронки, желоба, выпускные воронки | Select | Чистые / Необходима очистка | Чистые |
| 4.10.12 | Заключение | Select | Исправно, замечаний нет / Исправно, есть замечания / Неисправно | Исправно, замечаний нет |
| 4.10.13 | Типовые рекомендации | Multi-checkbox | Из справочника (категория «Сети водоснабжения и водоотведения») | — |
| 4.10.14 | Дополнительные рекомендации | Text | — | — |

#### 5.2.11. Страница 4.11 — Прибор учета ХВС

| № | Поле | Тип | Значения | По умолчанию | Обязательное |
|---|------|-----|----------|:------------:|:------------:|
| 4.11.1 | Модель счётчика | Text | — | — | ✅ |
| 4.11.2 | Номер счётчика | Text | — | — | ✅ |
| 4.11.3 | Наличие пломбы | Select | Да / Нет | Да | — |
| 4.11.4 | Показания | Number | — | — | ✅ |
| 4.11.5 | Заключение | Select | Исправно, замечаний нет / Исправно, есть замечания / Неисправно | Исправно, замечаний нет | — |
| 4.11.6 | Типовые рекомендации | Multi-checkbox | Из справочника (категория «Прибор учета ХВС») | — | — |
| 4.11.7 | Дополнительные рекомендации | Text | — | — | Условно |

#### 5.2.12. Страница 4.12 — Прибор учета ГВС

| № | Поле | Тип | Значения | По умолчанию | Обязательное |
|---|------|-----|----------|:------------:|:------------:|
| 4.12.1 | Модель счётчика | Text | — | — | ✅ |
| 4.12.2 | Номер счётчика | Text | — | — | ✅ |
| 4.12.3 | Наличие пломбы | Select | Да / Нет | Да | — |
| 4.12.4 | Показания | Number | — | — | ✅ |
| 4.12.5 | Заключение | Select | Исправно, замечаний нет / Исправно, есть замечания / Неисправно | Исправно, замечаний нет | — |
| 4.12.6 | Типовые рекомендации | Multi-checkbox | Из справочника (категория «Прибор учета ГВС») | — | — |
| 4.12.7 | Дополнительные рекомендации | Text | — | — | Условно |

#### 5.2.13. Страница 4.13 — Тепловые сети

| № | Поле | Тип | Значения | По умолчанию |
|---|------|-----|----------|:------------:|
| 4.13.1 | Наличие повреждений трубопровода теплоснабжения | Select | Да / Нет | Нет |
| 4.13.2 | Наличие коррозии на трубопроводах | Select | Да / Нет | Нет |
| 4.13.3 | Наличие свищей/протечек на трубопроводах | Select | Да / Нет | Нет |
| 4.13.4 | Наличие неисправностей крепежей | Select | Да / Нет | Нет |
| 4.13.5 | Наличие прочих неисправностей трубопроводов | Select | Да / Нет | Нет |
| 4.13.6 | Герметичность запорной, защитной и регулирующей арматуры | Select | Да / Нет | Да |
| 4.13.7 | Наличие завоздушивания системы | Select | Да / Нет | Нет |
| 4.13.8 | Контрольно-измерительные приборы исправны | Select | Да / Нет | Да |
| 4.13.9 | Температуры приборов отопления | Number | °C | — |
| 4.13.10 | Заключение | Select | Исправно, замечаний нет / Исправно, есть замечания / Неисправно | Исправно, замечаний нет |
| 4.13.11 | Типовые рекомендации | Multi-checkbox | Из справочника (категория «Тепловые сети») | — |
| 4.13.12 | Дополнительные рекомендации | Text | — | — |

### 5.3. Страница 5: «Фотофиксация»

**Макет:**
```
┌─────────────────────────────────────────────┐
│  Фотофиксация: [Вид оборудования]           │
│  Местоположение: [Тип помещения]            │
├─────────────────────────────────────────────┤
│  📷 Фото ДО выполнения работ:               │
│  [Сделать фото]  [Выбрать файл]             │
│  [Превью загруженного фото]                 │
├─────────────────────────────────────────────┤
│  📷 Фото ПОСЛЕ выполнения работ:            │
│  [Сделать фото]  [Выбрать файл]             │
│  [Превью загруженного фото]                 │
│  *(поле скрыто для оборудования с 1 фото)*  │
├─────────────────────────────────────────────┤
│  [Сохранить и вернуться в чек-лист]         │
│  [Назад]  [Сбросить задачу]                 │
└─────────────────────────────────────────────┘
```

**Логика:**
- Для оборудования, требующего 2 фото (СС, МСС, VRV, тепловая завеса, приточная установка, приточно-вытяжная установка, вытяжная установка): оба поля обязательны
- Для оборудования с 1 фото (РЩ, счетчики, сети): второе поле скрыто
- Кнопка «Сохранить» активна только после загрузки требуемого количества фото
- Кнопка «Сбросить задачу» очищает только текущую задачу

### 5.4. Страница 6: «Отправка отчета»

**Макет:**
```
┌─────────────────────────────────────────────┐
│  ✅ Отчет сформирован                       │
│  📦 OTCHET_Tverskaya_13_02-07-2026.zip      │
├─────────────────────────────────────────────┤
│  Кому: [zakazchik@example.com]              │
│  Копия (CC): [опционально]                  │
│  Тема: [Акт выполненных работ: ...]         │
│  Комментарий: [опционально]                 │
├─────────────────────────────────────────────┤
│  [📤 Отправить]  [💾 Скачать]  [✖ Отмена]   │
└─────────────────────────────────────────────┘
```

**Логика:**
- Email заказчика подставляется из справочника адресов
- Тема письма формируется автоматически: `Акт выполненных работ: {full_address} от {date}`
- При отсутствии интернета активна только кнопка «Скачать»

---

## 6. СПРАВОЧНИКИ

### 6.1. Справочник адресов

**Структура таблицы:**

| Поле | Тип | Описание |
|------|-----|----------|
| id | UUID | Первичный ключ |
| city | VARCHAR(100) | Город |
| street | VARCHAR(200) | Улица |
| house | VARCHAR(50) | Дом |
| building | VARCHAR(50) | Строение/корпус (опционально) |
| full_address | VARCHAR(500) | Полный адрес для отображения |
| object_code | VARCHAR(50) | Аналитический код объекта (уникальный, формат RU/77/001) |
| customer_email | VARCHAR(200) | Email заказчика (опционально) |
| is_deleted | BOOLEAN | Мягкое удаление (false по умолчанию) |
| created_at | TIMESTAMP | Дата создания |
| updated_at | TIMESTAMP | Дата обновления |

**Начальное наполнение:** ~230 адресов г. Москвы и Московской области (см. Приложение А).

### 6.2. Справочник видов оборудования

**Структура таблицы:**

| Поле | Тип | Описание |
|------|-----|----------|
| id | UUID | Первичный ключ |
| name | VARCHAR(100) | Наименование вида оборудования |
| code | VARCHAR(50) | Код для маркировки фото (транслит) |
| photos_required | INT | Количество требуемых фото (1 или 2) |
| is_active | BOOLEAN | Активен ли вид |
| specialization_req | TEXT (nullable) | Требуемая специализация: `vik`, `iszh`, `gpm`, `dgu` или `ibp` (NULL — доступно всем) |

**Начальное наполнение (39 записей: 38 активных + 1 деактивированный):**

| name | code | photos_required | is_active | specialization_req |
|------|------|:---------------:|:---------:|:------------------:|
| РЩ/ГРЩ | rsch | 1 | ✅ | iszh |
| ~~Вентиляционная установка~~ | vent | 2 | ❌ (деактивирован) | vik |
| Внутренний блок VRV | vrv_vn | 2 | ✅ | vik |
| Внутренний блок МСС | mssvn | 2 | ✅ | vik |
| Внутренний блок СС | splitvn | 2 | ✅ | vik |
| Наружный блок VRV | vrv_nar | 2 | ✅ | vik |
| Наружный блок МСС | mssnar | 2 | ✅ | vik |
| Наружный блок СС | splitnar | 2 | ✅ | vik |
| Тепловая завеса | teplozavesa | 2 | ✅ | vik |
| Приточная установка | pritochnaya | 2 | ✅ | vik |
| Приточно-вытяжная установка | pritochno-vytyzhnaya | 2 | ✅ | vik |
| Вытяжная установка | vytyzhnaya | 2 | ✅ | vik |
| Мобильный (напольный) кондиционер | cond_mobile | 2 | ✅ | vik |
| Прибор учета ГВС | schetchik_gvs | 1 | ✅ | iszh |
| Прибор учета ХВС | schetchik_hvs | 1 | ✅ | iszh |
| Прибор учета э/э | schetchik_electroshc | 1 | ✅ | iszh |
| Сети водоснабжения и водоотведения | seti_vodosnab | 1 | ✅ | iszh |
| Тепловые сети | teplovye_seti | 1 | ✅ | iszh |
| ИТП | itp | 1 | ✅ | iszh |
| Котёл газовый | boiler_gas | 1 | ✅ | iszh |
| Котёл жидкотопливный | boiler_liquid | 1 | ✅ | iszh |
| Котёл твёрдотопливный | boiler_solid | 1 | ✅ | iszh |
| Котёл электрический | boiler_elec | 1 | ✅ | iszh |
| Прибор учёта газопотребления | meter_gas | 1 | ✅ | iszh |
| Сололифт | sololift | 1 | ✅ | iszh |
| Шлагбаум / Рольставни | barrier_roller | 2 | ✅ | iszh |
| Автоматические двери / ворота | door_auto | 2 | ✅ | iszh |
| Кофе машина | coffee | 1 | ✅ | iszh |
| Пурифаер | purifier | 1 | ✅ | iszh |
| Куллер | cooler | 1 | ✅ | iszh |
| Аквариум | aquarium | 1 | ✅ | iszh |
| Пузырьковая панель | bubble_panel | 1 | ✅ | iszh |
| ДГУ | dgu | 2 | ✅ | dgu |
| МКГУ | mkgu | 2 | ✅ | dgu |
| ИБП | ibp | 1 | ✅ | ibp |
| Пассажирский лифт | lift_pass | 2 | ✅ | gpm |
| Грузовой лифт | lift_cargo | 2 | ✅ | gpm |
| Грузопассажирский лифт | lift_cargo_pass | 2 | ✅ | gpm |
| Подъёмные платформы для инвалидов | lift_invalid | 2 | ✅ | gpm |

> **Итого:** 38 активных видов + 1 деактивированный (`vent`). Вид `vent` сохранён для обратной совместимости с ранее созданными задачами. Специализации: ВиК — 13 активных, ИСЖ — 20, ГПМ — 4, ДГУ — 2, ИБП — 1.

### 6.3. Справочник типов помещений

**Структура таблицы:**

| Поле | Тип | Описание |
|------|-----|----------|
| id | UUID | Первичный ключ |
| name | VARCHAR(100) | Наименование типа помещения |
| code | VARCHAR(50) | Код для маркировки фото (транслит) |

**Начальное наполнение:**

| name | code |
|------|------|
| Электрощитовая | electroshc |
| Серверная | server |
| Клиентский зал | kassovaya |
| Комната приёма пищи | kpp |
| Санузел | sanitarnyj_uzel |
| Кровля | krovlya |
| Фасад | fasad |
| Зона самообслуживания | zona_samoobsl |
| Крыльцо | kryltso |
| Тепловой узел | teplovoj_uzel |
| Касса | kassa |
| КХЦ | khc |

### 6.4. Справочник типовых рекомендаций

**Структура таблицы:**

| Поле | Тип | Описание |
|------|-----|----------|
| id | UUID | Первичный ключ |
| equipment_type_id | UUID | FK на справочник видов оборудования |
| text | TEXT | Текст рекомендации |
| sort_order | INT | Порядок отображения |
| is_active | BOOLEAN | Активна ли рекомендация |

**Начальное наполнение:** 38 активных категорий + 1 деактивированная, ~150 рекомендаций (см. Приложение Б).

### 6.5. Справочник сезонов

| Период | Значение |
|--------|----------|
| 01.04 – 31.10 | Лето |
| 01.11 – 31.03 | Зима |

**Алгоритм определения:**
```javascript
function determineSeason(date) {
  const d = new Date(date);
  const month = d.getMonth() + 1; // 1..12
  const day = d.getDate();
  
  // Лето: 1 апреля – 31 октября
  if ((month > 4 && month < 10) || 
      (month === 4 && day >= 1) || 
      (month === 10 && day <= 31)) {
    return 'Лето';
  }
  return 'Зима';
}
```

### 6.6. Привязка «Объект-Оборудование» (object_equipment)

Модуль хранит информацию о том, какое оборудование физически установлено на каждом объекте (адресе). Используется для автозаполнения чек-листа инженера при создании визита.

**Структура таблицы:**

| Поле | Тип | Описание |
|------|-----|----------|
| id | UUID | Первичный ключ |
| address_id | UUID | FK на справочник адресов |
| equipment_type_code | VARCHAR(50) | Код вида оборудования |
| room_type_code | VARCHAR(50) | Код типа помещения (**опционально**, может быть NULL) |
| brand | VARCHAR(200) | Марка оборудования (опционально) |
| model | VARCHAR(200) | Модель оборудования (опционально) |
| serial_number | VARCHAR(200) | Серийный номер (опционально) |
| location_description | TEXT | Описание расположения на объекте (опционально) |
| is_outdoor_unit | BOOLEAN | Признак наружного блока кондиционера (false по умолчанию) |
| is_active | BOOLEAN | Активна ли привязка (true по умолчанию) |
| room_confirmed_at | TIMESTAMP | Дата первичного уточнения помещения инженером (опционально) |
| room_confirmed_by | UUID | FK на пользователя, уточнившего помещение (опционально) |
| created_at | TIMESTAMP | Дата создания |
| updated_at | TIMESTAMP | Дата обновления |

**Уникальные ограничения:**
- `(address_id, equipment_type_code, serial_number)` — одна и та же единица оборудования с серийным номером не может быть привязана к объекту дважды
- `(address_id, equipment_type_code, location_description)` — для записей без серийного номера уникальность обеспечивается по описанию расположения

**Поведение при добавлении оборудования в визит:**
- При нажатии кнопки «+ Добавить оборудование» открывается модальное окно с тремя вкладками: «📍 Уровень помещения», «🏠 Уровень объекта», «➕ Добавить новое»
- Оборудование распределяется по вкладкам на основании наличия `room_type_code` в записи `object_equipment`:
  - Записи с указанным `room_type_code` → вкладка «Уровень помещения» (группируются по помещению)
  - Записи без `room_type_code` → вкладка «Уровень объекта» (плоский список)
- На обеих вкладках отображаются только активные записи `object_equipment` для данного `address_id`, отфильтрованные по специализации инженера
- Записи, уже добавленные в текущий визит (через поле `object_equipment_id` в таблице `tasks`), **исключаются** из списка — это предотвращает дубликаты
- Список помещений для вкладки «Уровень помещения» загружается через `GET /api/refs/object-equipment/rooms?address_id=...&exclude_visit_id=...`
- Для записей без указанного помещения (`room_type_code = NULL`) отображается метка «⚠️ Помещение не указано»
- При выборе таких записей появляется inline-список для выбора типа помещения из справочника
- Кнопка «Добавить» неактивна, пока для всех отмеченных единиц без помещения не выбран тип
- При добавлении: для оборудования без помещения вызывается `PATCH /api/refs/object-equipment/:id/room` — помещение сохраняется в `object_equipment` с заполнением `room_confirmed_at` и `room_confirmed_by`
- Инженер отмечает галочками нужные позиции и нажимает «Добавить» — для каждой создаётся задача с предзаполненными полями
- Если все записи уже добавлены, соответствующая вкладка показывает сообщение «Всё оборудование уже добавлено в визит»
- На вкладке «➕ Добавить новое» инженер может добавить оборудование вручную (не из привязки)
- Новые строки, добавленные вручную, не сохраняются в `object_equipment` автоматически (для этого инженер отмечает чекбокс «Предложить добавить в привязку объекта»)

### 6.7. Предложения оборудования (equipment_proposals)

Модуль хранит предложения инженеров о добавлении нового оборудования в привязку «Объект-Оборудование». Предложения создаются, когда инженер обнаруживает на объекте оборудование, которое ещё не привязано.

**Структура таблицы:**

| Поле | Тип | Описание |
|------|-----|----------|
| id | UUID | Первичный ключ |
| address_id | UUID | FK на справочник адресов |
| equipment_type_code | VARCHAR(50) | Код вида оборудования |
| room_type_code | VARCHAR(50) | Код типа помещения (опционально) |
| brand | VARCHAR(100) | Марка оборудования |
| model | VARCHAR(100) | Модель оборудования |
| serial_number | VARCHAR(100) | Серийный номер |
| location_description | TEXT | Описание расположения |
| proposed_by_id | UUID | FK на пользователя (инженер), предложившего оборудование |
| status | VARCHAR(20) | Статус предложения: `pending`, `approved`, `rejected` |
| reviewed_by_id | UUID | FK на пользователя (администратор), рассмотревшего предложение (опционально) |
| reviewed_at | TIMESTAMP | Дата рассмотрения |
| created_at | TIMESTAMP | Дата создания предложения |
| updated_at | TIMESTAMP | Дата обновления |

**Жизненный цикл:**
1. Инженер добавляет оборудование в визит и отмечает чекбокс «Предложить добавить в привязку объекта» → создаётся запись со статусом `pending`
2. Администратор видит предложение в списке `pending`-предложений
3. Администратор утверждает → статус меняется на `approved`, запись автоматически копируется в `object_equipment` с `is_active = true`
4. Администратор отклоняет → статус меняется на `rejected`, указывается причина в `review_comment`

### 6.8. Маппинг специализаций

Каждый инженер имеет одну или несколько специализаций, определяющих набор обслуживаемого оборудования.

| Специализация | Поле в БД | Коды оборудования | Описание |
|:-------------:|-----------|-------------------|----------|
| **ВиК** | `specialization_vik` | ~~vent~~ (деактивирован), teplozavesa, pritochnaya, pritochno-vytyzhnaya, vytyzhnaya, vrv_vn, vrv_nar, mssvn, mssnar, splitvn, splitnar, cond_mobile | Вентиляция и кондиционирование (12 активных + 1 деактивированный) |
| **ИСЖ** | `specialization_iszh` | rsch, schetchik_gvs, schetchik_hvs, schetchik_electroshc, seti_vodosnab, teplovye_seti, itp, boiler_gas, boiler_liquid, boiler_solid, boiler_elec, meter_gas, sololift, barrier_roller, door_auto, coffee, purifier, cooler, aquarium, bubble_panel | Инженерные сети и здания (20 типов) |
| **ГПМ** | `specialization_gpm` | lift_pass, lift_cargo, lift_cargo_pass, lift_invalid | Грузоподъёмные механизмы (4 типа) |
| **ДГУ** | `specialization_dgu` | dgu, mkgu | Дизель-генераторные установки (2 типа) |
| **ИБП** | `specialization_ibp` | ibp | Источники бесперебойного питания (1 тип) |

**Правила:**
- Значения по умолчанию при создании инженера: `specialization_vik = false`, `specialization_iszh = true`
- Инженер может изменить свою специализацию через ЛК (страница `/profile`)
- ТМ/Администратор могут изменить специализацию подчинённого инженера
- Если ни одна специализация не выбрана — показывается блокирующий экран (SpecializationGate)
- Задачи в визите фильтруются по специализации инженера
- Оборудование объекта фильтруется по специализации при автозаполнении
- Специализация инженера отображается в PDF-отчёте

### 6.9. Производители (`manufacturers`)

Справочник производителей (изготовителей) оборудования. Используется для формирования полного названия модели.

**Структура таблицы:**

| Поле | Тип | Описание |
|------|-----|----------|
| id | UUID | Первичный ключ |
| name | VARCHAR(200) | Название производителя (уникальное) |
| country | VARCHAR(100) | Страна производства (опционально) |
| is_active | BOOLEAN | Активен ли производитель (true по умолчанию) |
| created_at | TIMESTAMP | Дата создания |
| updated_at | TIMESTAMP | Дата обновления |

**API:**
- `GET /api/admin/manufacturers` — список с пагинацией и поиском
- `POST /api/admin/manufacturers` — создание
- `PUT /api/admin/manufacturers/:id` — редактирование
- `DELETE /api/admin/manufacturers/:id` — деактивация
- `GET /api/refs/manufacturers` — список активных (для клиента)
- `POST /api/admin/import/manufacturers` — импорт из CSV (формат: `name, country`)

### 6.10. Модели оборудования (`models`)

Справочник моделей оборудования с привязкой к виду оборудования и производителю. Поддерживает модерацию: инженер может предложить новую модель, администратор — утвердить или отклонить.

**Структура таблицы:**

| Поле | Тип | Описание |
|------|-----|----------|
| id | UUID | Первичный ключ |
| equipment_type_id | UUID | FK на виды оборудования |
| manufacturer_id | UUID | FK на производителей |
| model_name | VARCHAR(200) | Название модели |
| full_model_name | VARCHAR(400) | Полное название (Производитель + Модель) |
| status | VARCHAR(20) | Статус: `approved`, `pending`, `rejected` |
| submitted_by | UUID | FK на пользователя, предложившего модель (опционально) |
| submitted_at | TIMESTAMP | Дата предложения |
| reviewed_by | UUID | FK на пользователя, рассмотревшего модель (опционально) |
| reviewed_at | TIMESTAMP | Дата рассмотрения |
| rejection_reason | TEXT | Причина отклонения (опционально) |
| created_at | TIMESTAMP | Дата создания |
| updated_at | TIMESTAMP | Дата обновления |

**Уникальный индекс:** `(equipment_type_id, manufacturer_id, model_name)`

**API:**
- `GET /api/admin/models` — список с пагинацией, фильтрами по статусу/виду/производителю
- `POST /api/admin/models` — создание модели
- `PUT /api/admin/models/:id` — редактирование
- `PUT /api/admin/models/:id/approve` — утверждение (админ)
- `PUT /api/admin/models/:id/reject` — отклонение с причиной (админ)
- `GET /api/refs/models/search?equipment_type_id=&query=` — поиск с автодополнением (только approved)
- `POST /api/admin/import/models` — импорт из CSV (формат: `eq_type, manufacture, model`)

### 6.11. Голосовой ввод (Speech-to-Text)

Функция голосового ввода текста в поле «Дополнительные рекомендации» на странице параметров задачи. Использует Web Speech API (нативный браузерный API, без сторонних сервисов).

**Реализация:**
- Кнопка микрофона (40×40px) размещена справа от лейбла «Дополнительные рекомендации»
- Механика: клик — начало записи, повторный клик — остановка
- Распознанный текст добавляется в конец существующего текста через пробел
- Язык распознавания: `ru-RU`
- Непрерывный режим (`continuous = true`) для длинных рекомендаций

**Состояния кнопки:**
- **Ожидание** — серый микрофон на светлой подложке
- **Запись** — красный микрофон с пульсацией и обводкой
- **Ошибка** — красный перечёркнутый микрофон с тултипом причины
- **Недоступно** — полупрозрачный микрофон (офлайн-режим)

**Офлайн-режим:** Web Speech API требует интернет-соединение. При отсутствии сети кнопка становится неактивной с тултипом.

**Совместимость:** Chrome/Edge (Android, Desktop), Safari 14.5+ (iOS).

**Файлы реализации:**
- `client/src/hooks/useSpeechRecognition.ts` — React-хук для управления распознаванием
- `client/src/components/VoiceInputButton.tsx` — компонент кнопки микрофона

### 6.12. Фонарик (Torch)

Функция управления встроенным фонариком мобильного устройства в интерфейсе визита. Использует Web Torch API (`getUserMedia` + `applyConstraints({torch: true})`).

**Реализация:**
- Кнопка-пиктограмма (40×40px) размещена в Header визита справа от заголовка
- Отображается только на мобильных устройствах (≤768px) при наличии поддержки Torch API
- Механика: клик — включение, повторный клик — выключение
- Результат `isSupported()` кэшируется — камера не запрашивается при каждой проверке

**Автоматическое выключение:**
- При сворачивании приложения (`visibilitychange`)
- При завершении сессии (logout)

**Уведомления:**
- Toast при заряде батареи <5% (если Battery API доступен)
- Toast при работе фонарика >15 минут (предупреждение о перегреве)

**Fallback для iOS:** Модальное окно с инструкцией по включению фонарика через Пункт управления.

**Файлы реализации:**
- `client/src/hooks/useTorch.ts` — React-хук для управления фонариком
- `client/src/components/TorchButton.tsx` — компонент кнопки фонарика
- `client/src/types/torch.d.ts` — TypeScript-типы для Torch API и Battery API

---

### 6.13. Модерация привязки оборудования к помещениям

Механизм гибкого управления привязкой оборудования к помещениям с модерацией и TTL.

**Концепция:** Инженер может переносить оборудование между помещениями и добавлять новое оборудование прямо из визита. Изменения вступают в силу немедленно, но требуют подтверждения администратором в течение 30 дней (TTL).

**Статусы записей:**

| Статус | Описание | TTL |
|--------|----------|-----|
| `confirmed` | Подтверждено (создано админом/импортом или подтверждено после модерации) | Бессрочно |
| `pending` | Добавлено инженером, ждёт подтверждения | 30 дней |
| `rejected` | Отклонено администратором или отменено инженером | — |
| `expired` | Не подтверждено в срок, деактивировано cron-задачей | — |

**Сценарии:**

**А. Перенос оборудования между помещениями:**
1. Инженер выбирает помещение в модалке добавления оборудования
2. Видит секцию «Оборудование из других помещений» с кнопкой «Перенести»
3. При нажатии «Перенести» — создаётся `equipment_proposal` с `request_type = 'room_change'`, `object_equipment.roomTypeCode` обновляется немедленно
4. Администратор подтверждает или отклоняет (при отклонении — откат `roomTypeCode`)

**Б. Добавление нового оборудования:**
1. Инженер заполняет форму (вид, помещение, изготовитель, модель, SN)
2. Может добавить нового изготовителя или модель (создаются со статусом `pending`)
3. Создаётся `equipment_proposal` со статусом `pending` и `pending_until = NOW() + 30 дней`
4. Администратор подтверждает (создаётся `object_equipment`) или отклоняет

**В. Модерация (администратор):**
- Дашборд с фильтрами: тип запроса, объект, инженер, истекающие скоро
- Массовые действия: подтвердить/отклонить несколько записей
- Отклонение с указанием причины (опционально)

**Г. Автоматическое удаление (cron, ежедневно в 03:00):**
- Помечает истёкшие proposals как `expired`
- Откатывает `room_change` (восстанавливает `roomTypeCode`)
- Деактивирует `new_equipment` (`is_active = FALSE`)
- Создаёт уведомления инженерам (без дублей)

**Д. Уведомления:**
- Колокольчик в header (badge с количеством непрочитанных)
- Типы: `proposal_created`, `proposal_approved`, `proposal_rejected`, `proposal_expiring_soon`, `proposal_expired`
- Получатели: администраторы + ТМ привязанных объектов

**Файлы реализации:**
- `server/prisma/schema.prisma` — расширение `EquipmentProposal`, `ObjectEquipment`, модель `Notification`
- `server/src/routes/proposals.ts` — API модерации (9 эндпоинтов)
- `server/src/routes/notifications.ts` — API уведомлений
- `server/src/routes/refs.ts` — `GET /object-equipment/other-rooms`
- `server/src/services/cron.ts` — cron-задача обработки TTL
- `client/src/pages/admin/AdminProposals.tsx` — дашборд модерации
- `client/src/components/NotificationBell.tsx` — компонент уведомлений
- `client/src/pages/VisitPage.tsx` — секция переноса оборудования
- `shared/types/index.ts` — типы `ConfirmationStatus`, `ProposalStatus`, `RequestType`, `NotificationType`

---

## 7. ТРЕБОВАНИЯ К ЭКСПОРТУ

### 7.1. Формирование PDF-отчета

**Структура документа:**

```
┌─────────────────────────────────────────────┐
│           АКТЫ ВЫПОЛНЕННЫХ РАБОТ            │
├─────────────────────────────────────────────┤
│ Общие сведения:                             │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│ Адрес объекта:     г. Москва, ул. Тверская, │
│                    д.13                     │
│ Дата проведения:   02.07.2026              │
│ Время начала:      10:30                   │
│ Время окончания:   12:45                   │
│ Сезон:             Лето                    │
│ Инженер:           Иванов П.С.             │
├─────────────────────────────────────────────┤
│ 1. Внутренний блок СС (в серверной)         │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│ 📍 Местоположение: Серверная                │
│ 🔧 Контролируемые параметры:                │
│    • Работоспособность: Удовлетворительно   │
│    • Наличие посторонних шумов: Нет         │
│    • Температура помещения: 18°C            │
│    • ...                                    │
│ 📸 Фотофиксация:                            │
│    [ФОТО 1] 01_splitvn_server_before        │
│    [ФОТО 2] 01_splitvn_server_after         │
│ ✅ Заключение: Исправно, замечаний нет      │
│ 📊 Рекомендации:                            │
│    • Выполнить чистку фильтров...           │
│    • Прочистить дренажную систему...        │
├─────────────────────────────────────────────┤
│ ...                                         │
├─────────────────────────────────────────────┤
│ ПОДПИСЬ ИНЖЕНЕРА: ____________ /Иванов П.С./│
└─────────────────────────────────────────────┘
```

**Технические требования:**
- Формат: PDF/A-1b (для долгосрочного архивирования)
- Размер страницы: A4
- Шрифт: Arial / Roboto, 11pt
- Фотографии: встраиваются в PDF с разрешением 150 DPI
- Имя файла: `OTCHET_{street}_{house}_{date}.pdf`

### 7.2. Формирование ZIP-архива

**Структура архива:**
```
OTCHET_Tverskaya_13_02-07-2026.zip
├── OTCHET_Tverskaya_13_02-07-2026.pdf
└── Photos/
    ├── 01_splitvn_server_12345678_before.jpg
    ├── 01_splitvn_server_12345678_after.jpg
    ├── 02_splitvn_server_12345679_before.jpg
    ├── 02_splitvn_server_12345679_after.jpg
    ├── 03_mssvn_server_45678901_before.jpg
    ├── 03_mssvn_server_45678901_after.jpg
    ├── 04_rsch_electroshc_nosn_before.jpg
    ├── 05_schetchik_electroshc_electroshc_98765432_before.jpg
    └── ...
```

**Шаблон имени файла:**
- Для индивидуальных задач: `{№}_{тип_оборудования}_{помещение}_{момент}.jpg`
- Для групповых задач (климат в помещении): `{№}_{тип_оборудования}_{помещение}_{серийный_номер}_{момент}.jpg`
- Для групповых задач (наружные блоки): `{№}_{тип_оборудования}_object_{серийный_номер}_{момент}.jpg`
- Если серийный номер отсутствует — используется `nosn`

> **Важно:** Все части имени файлаsanitize-ся — символы `/` и `\` заменяются на `_` для предотвращения создания поддиректорий.

**Имя архива:** `OTCHET_{street}_{house}_{date}.zip`

### 7.3. Отправка по email

**Требования:**
- Протокол: SMTP / SMTPS
- Размер вложения: до 25 МБ (при превышении — разбивка на несколько писем или использование облачного хранилища)
- Тема письма: `Акт выполненных работ: {full_address} от {date}`
- Тело письма: шаблон с краткой сводкой (адрес, дата, количество задач)
- Резервный сценарий: скачивание ZIP на устройство

### 7.4. Унифицированный сводный отчёт

**Структура документа (единая для отчёта за период и отчёта по объектам):**

```
┌─────────────────────────────────────────────┐
│        СВОДНЫЙ ОТЧЁТ ЗА ПЕРИОД              │
│        (или ОТЧЁТ ПО ОБЪЕКТАМ)              │
├─────────────────────────────────────────────┤
│ Период: 01.06.2026 — 30.06.2026            │
│ Дата формирования: 01.07.2026              │
│ Сформировал: Петрова А.В. (ТМ)             │
├─────────────────────────────────────────────┤
│ Статистика: визитов / задач / замечаний     │
├─────────────────────────────────────────────┤
│ ► Объект 1: г. Москва, ул. Тверская, д.13  │
│   ├─ Визит 1: 05.06.2026, Иванов П.С. (ВиК)│
│   │  ├─ Задача 1: Параметры, фото (превью),│
│   │  │  заключение, рекомендации            │
│   │  └─ Задача 2: ...                       │
│   └─ Визит 2: ...                           │
│ ► Объект 2: г. Москва, ул. Арбат, д.33     │
│   └─ ...                                    │
├─────────────────────────────────────────────┤
│ Приложения: Копии актов с подписью          │
│ (если загружены сканы)                      │
└─────────────────────────────────────────────┘
```

**Технические требования:**
- Генерация: `POST /api/reports/summary-generate` (унифицированный эндпоинт)
- Превью фото: `sharp` resize width=400px, quality=85, JPEG base64 inline
- Лимит фото: 200 штук; при превышении — упрощённый режим (текстовые метки)
- Сканы актов: сжатие изображений (width=1200px, quality=80), merge PDF через `pdf-lib`
- Титульный лист: название отчёта, период/объекты, дата, ФИО сформировавшего (из JWT)
- Имя файла: `Svodnyj_otchet_{dateFrom}_{dateTo}.pdf` или `Otchet_po_obektam_{dateFrom}_{dateTo}.pdf`

---

## 8. НЕФУНКЦИОНАЛЬНЫЕ ТРЕБОВАНИЯ

### 8.1. Архитектура
- **Тип приложения:** PWA (Progressive Web App)
- **Frontend:** React / Vue.js + Service Workers + IndexedDB
- **Backend:** Node.js / Python (FastAPI)
- **База данных:** PostgreSQL
- **Генерация PDF:** jsPDF / Puppeteer / WeasyPrint
- **Генерация ZIP:** JSZip (на клиенте) или архивация на сервере
- **Email:** SMTP / SendGrid / Mailgun

### 8.2. Производительность
- Время загрузки страницы: не более 3 секунд (при 4G)
- Время формирования PDF: не более 10 секунд (для 20 задач)
- Время формирования ZIP: не более 15 секунд (для 40 фото)
- Офлайн-работа: полная функциональность без интернета

### 8.2.1. Офлайн-режим

**Требования к офлайн-работе:**
- Приложение должно функционировать без подключения к интернету
- Все действия инженера (создание визита, заполнение параметров, фотофиксация) сохраняются локально (IndexedDB)
- При восстановлении соединения — автоматическая синхронизация с сервером
- Фотографии хранятся как Blob в IndexedDB до момента синхронизации
- Очередь мутаций с повторными попытками (до 5 раз)
- Визуальный индикатор статуса: «Офлайн» / «Синхронизация...» / «Онлайн»
- Service Worker кэширует API-ответы (NetworkFirst, timeout 3с, TTL 7 дней)

**Технологии:**
- Dexie.js (IndexedDB wrapper)
- Workbox (Service Worker)
- navigator.onLine API

### 8.3. Безопасность
- Аутентификация: JWT-токены
- Авторизация: ролевая модель (п. 3.2)
- Шифрование данных: TLS 1.3
- Хранение паролей: bcrypt
- Защита от XSS, CSRF, SQL-инъекций
- Соответствие 152-ФЗ (персональные данные)

### 8.4. Масштабируемость
- Поддержка до 500 одновременных пользователей
- Хранение данных: не менее 5 лет
- Резервное копирование: ежедневно

### 8.5. Совместимость
- Браузеры: Chrome 90+, Safari 14+, Firefox 88+, Edge 90+
- Устройства: смартфоны (iOS 14+, Android 10+), планшеты
- Разрешение экрана: от 320×568 до 1920×1080

---

## 9. СТРУКТУРА ДАННЫХ

### 9.1. ER-диаграмма (основные сущности)

```
┌──────────────┐       ┌──────────────┐       ┌──────────────┐
│    Users     │       │   Addresses  │       │   Visits     │
├──────────────┤       ├──────────────┤       ├──────────────┤
│ id (PK)      │       │ id (PK)      │       │ id (PK)      │
│ full_name    │       │ city         │◄──────│ address_id   │
│ email        │       │ street       │       │ user_id      │
│ role         │       │ house        │       │ date_start   │
│ password     │       │ building     │       │ time_start   │
│ is_active    │       │ full_address │       │ time_end     │
└──────────────┘       │ object_code  │       │ season       │
                       │ customer_email│      │ status       │
                       └──────────────┘
                                              └──────┬───────┘
                                                     │
                                                     │ 1:N
                                                     ▼
┌──────────────┐       ┌──────────────┐       ┌──────────────┐
│  Equipment   │       │    Tasks     │       │    Photos    │
│    Types     │       ├──────────────┤       ├──────────────┤
├──────────────┤       │ id (PK)      │       │ id (PK)      │
│ id (PK)      │◄──────│ visit_id (FK)│◄──────│ task_id (FK) │
│ name         │       │ equipment_   │       │ file_path    │
│ code         │       │  type_id(FK) │       │ moment       │
│ photos_req.  │       │ room_type_id │       │ created_at   │
│ specialization│      │ object_equip.│       └──────────────┘
└──────────────┘       │   _id (FK?)  │
                       │ comment      │       ┌──────────────┐
                       │ brand        │       │  Room Types  │
                       │ model        │       ├──────────────┤
                       │ serial_number│       │ id (PK)      │
                       │ parameters   │       │ name         │
                       │ recommendat. │       │ code         │
                       │ conclusion   │       └──────────────┘
                       └──────────────┘
┌──────────────┐       ┌──────────────────┐
│Recommendations│       │ object_equipment │
├──────────────┤       ├──────────────────┤
│ id (PK)      │       │ id (PK)          │
│ equipment_   │       │ address_id (FK)  │
│  type_id(FK) │       │ equipment_type_  │
│ text         │       │  code            │
│ sort_order   │       │ room_type_code   │
└──────────────┘       │ brand            │
                       │ model            │
                       │ serial_number    │
                       │ location_descr.  │
                       │ is_active        │
                       └──────────────────┘
```

### 9.2. Основные таблицы

**Users** — пользователи системы. Поле `role` имеет значения: `engineer`, `tm`, `admin`.
**Addresses** — справочник адресов
**RoomTypes** — типы помещений
**EquipmentTypes** — виды оборудования
**Recommendations** — типовые рекомендации
**Visits** — визиты инженера. Дополнительные поля: `assigned_by_id` (UUID — кем назначен), `assigned_at` (TIMESTAMP — дата назначения), `sent_by_engineer_at` (TIMESTAMP — дата отправки инженером), `sent_by_tm_at` (TIMESTAMP — дата отправки ТМ), `tm_corrected` (BOOLEAN — откорректирован ТМ), `is_deleted` (BOOLEAN — удалён ли визит), `deleted_by_id` (UUID — кем удалён), `deleted_at` (TIMESTAMP — дата удаления).

Поле `status` (VisitStatus) принимает следующие значения:
- `planned` — Запланировано (ТМ назначил визит инженеру)
- `not_started` — Не начато
- `in_progress` — В работе
- `completed` — Выполнено
- `sent` — Отправлен (общий статус)
- `sent_by_engineer` — Отправлен инженером
- `sent_by_tm` — Отправлен ТМ
- `corrected_by_tm` — Откорректирован ТМ

**Tasks** — задачи в чек-листе. Поля: `task_type` (VARCHAR, default 'individual' — тип задачи: 'individual' или 'group_climate'), `object_equipment_id` (UUID, nullable — ссылка на `object_equipment`, если задача создана из привязки объекта), `room_type_code` (VARCHAR, nullable — код помещения для групповых задач), `comment` (текстовый комментарий, бывшее `location`), `brand` (марка оборудования), `model` (модель оборудования), `serial_number` (серийный номер). Для групповых задач (`task_type = 'group_climate'`) общие параметры хранятся в `parameters`, а единицы оборудования — в связанной таблице `task_equipment_items`.
**Photos** — привязка фото к задачам. Для индивидуальных задач — через `task_id`. Для групповых задач — через `task_equipment_item_id` (каждое фото привязано к конкретной единице в групповой задаче).

**Таблица task_equipment_items** — единицы оборудования в групповой задаче:

| Поле | Тип | Описание |
|------|-----|----------|
| id | UUID | Первичный ключ |
| task_id | UUID | FK на tasks (групповая задача) |
| object_equipment_id | UUID | FK на object_equipment (конкретная единица) |
| status | VARCHAR | Статус единицы: 'ok' / 'not_ok' (nullable) |
| sort_order | INT | Порядок отображения |
| created_at | TIMESTAMP | Дата создания |

Уникальный: `(task_id, object_equipment_id)`.

**Таблица tm_objects** — привязка объектов к ТМ:

| Поле | Тип | Описание |
|------|-----|----------|
| id | UUID | Первичный ключ |
| tm_id | UUID | Ссылка на ТМ |
| address_id | UUID | Ссылка на адрес объекта |
| created_at | TIMESTAMP | Дата создания |

**Таблица tm_engineers** — привязка инженеров к ТМ:

| Поле | Тип | Описание |
|------|-----|----------|
| id | UUID | Первичный ключ |
| tm_id | UUID | Ссылка на ТМ |
| engineer_id | UUID | Ссылка на инженера |
| created_at | TIMESTAMP | Дата создания |

**Таблица object_equipment** — привязка оборудования к объектам (адресам). Структура описана в разделе 6.6. Используется для автозаполнения чек-листа при создании визита.

**Таблица equipment_proposals** — предложения инженеров по добавлению оборудования в привязку объекта. Структура описана в разделе 6.7. Статусы: `pending`, `approved`, `rejected`.

### 9.3. Таблицы локального хранилища (IndexedDB / Dexie)

| Таблица | Назначение |
|---------|-----------|
| visits | Локальные визиты (с флагом dirty для синхронизации) |
| tasks | Локальные задачи (привязаны к visitLocalId) |
| photos | Фото как Blob (до синхронизации с сервером) |
| syncQueue | Очередь операций для отправки на сервер |
| cachedRefs | Кэш справочников (адреса, оборудование, помещения) |

---

## 10. КРИТЕРИИ ПРИЕМКИ

### 10.1. Функциональные критерии

| № | Критерий | Метод проверки |
|---|----------|----------------|
| 1 | Инженер может создать визит, выбрав адрес из справочника | Тест-кейс TC-001 |
| 2 | Сезон определяется автоматически по дате (Лето/Зима) | Тест-кейс TC-002 |
| 3 | Инженер может добавить несколько задач в чек-лист за один проход | Тест-кейс TC-003 |
| 4 | Система требует 2 фото для климатического оборудования и 1 фото для остального | Тест-кейс TC-004 |
| 5 | Рекомендации выбираются чек-боксами из справочника | Тест-кейс TC-005 |
| 6 | Кнопка «Сбросить» очищает только текущую задачу | Тест-кейс TC-006 |
| 7 | Приложение формирует PDF-отчет по шаблону | Тест-кейс TC-007 |
| 8 | Приложение формирует ZIP-архив с маркированными фото | Тест-кейс TC-008 |
| 9 | Отчет отправляется по email заказчику | Тест-кейс TC-009 |
| 10 | Приложение работает без интернета (офлайн-режим) | Тест-кейс TC-010 |
| 11 | При появлении сети данные синхронизируются автоматически | Тест-кейс TC-011 |
| 12 | Предупреждение о 152-ФЗ отображается при фото в клиентских зонах | Тест-кейс TC-012 |
| 13 | ТМ видит только визиты своих инженеров | Тест-кейс TC-013 |
| 14 | ТМ может переназначать визиты между инженерами своей группы | Тест-кейс TC-014 |
| 15 | ТМ не может видеть визиты инженеров из других групп | Тест-кейс TC-015 |
| 16 | ТМ может формировать и отправлять отчёты по визитам своих инженеров | Тест-кейс TC-016 |
| 17 | Soft delete: удалённый визит скрыт из активного списка, данные сохранены для аудита | Тест-кейс TC-017 |
| 18 | Приложение работает без интернета: создание визита, задач, загрузка фото | Тест-кейс TC-018 |
| 19 | При восстановлении связи данные автоматически синхронизируются | Тест-кейс TC-019 |
| 20 | Баннер статуса отображает текущее состояние (офлайн/синхронизация/онлайн) | Тест-кейс TC-020 |
| 21 | Фото сохраняются локально и загружаются на сервер при синхронизации | Тест-кейс TC-021 |
| 22 | При выборе помещения с несколькими единиками климата формируется одна групповая задача | Тест-кейс TC-071 |
| 23 | В групповой задаче отображаются общие параметры и список единиц с индивидуальными статусами | Тест-кейс TC-072 |
| 24 | Для каждой единицы в групповой задаче можно загрузить фото ДО/ПОСЛЕ | Тест-кейс TC-073 |
| 25 | Кнопка «Сохранить» неактивна без заполнения статусов и фото для всех единиц | Тест-кейс TC-074 |
| 26 | Наружные блоки группируются в отдельную задачу по уровню объекта | Тест-кейс TC-075 |
| 27 | В PDF-отчете групповая задача отображается с таблицей единиц и индивидуальными статусами | Тест-кейс TC-076 |
| 28 | Фото в ZIP-архиве маркируются с учетом серийного номера единицы | Тест-кейс TC-077 |
| 29 | Серийные номера с символами `/` не создают поддиректории при сохранении фото | Тест-кейс TC-078 |
| 30 | Автозаполнение полей счётчика из привязки объекта работает корректно | Тест-кейс TC-079 |
| 31 | Запрещена загрузка одного и того же фото для разных единиц в групповой задаче | Тест-кейс TC-165 |
| 32 | Админ может управлять специализацией инженера (ВиК/ИСЖ/ГПМ/ДГУ/ИБП) при создании и редактировании | Тест-кейс TC-167 |
| 33 | При смене роли инженера на ТМ специализация сбрасывается | Тест-кейс TC-168 |
| 34 | Поиск пользователей по ФИО/email работает (case-insensitive, частичное совпадение) | Тест-кейс TC-169 |
| 35 | Сводный отчёт за период и отчёт по объектам имеют единую структуру с титульным листом | Тест-кейс TC-170 |
| 36 | В сводном отчёте можно выбрать несколько объектов (мультивыбор) | Тест-кейс TC-171 |
| 37 | В сводный PDF встроены превью фотографий (не текстовые метки) | Тест-кейс TC-172 |
| 38 | Можно загрузить сканы актов (JPG/PNG/PDF), они сжимаются и встраиваются в PDF | Тест-кейс TC-173 |
| 39 | При превышении 200 фото отчёт формируется в упрощённом режиме | Тест-кейс TC-174 |

### 10.2. Нефункциональные критерии

| № | Критерий | Метод проверки |
|---|----------|----------------|
| 1 | Время загрузки страницы ≤ 3 сек | Нагрузочное тестирование |
| 2 | Время формирования PDF ≤ 10 сек | Тест производительности |
| 3 | Поддержка 500 одновременных пользователей | Стресс-тест |
| 4 | Соответствие 152-ФЗ | Аудит безопасности |
| 5 | Работа на iOS 14+ и Android 10+ | Кросс-браузерное тестирование |

### 10.3. Критерии приемки по UX

- Инженер с опытом работы может освоить приложение за 15 минут
- Время обслуживания 1 единицы оборудования сокращено на 30–40%
- Приложение не требует обучения для базовых операциям

---

## 11. ИНТЕГРАЦИЯ ВНЕШНИХ ЗАЯВОК

### 11.1. Назначение модуля

Модуль обеспечивает импорт перечня заявок из внешнего сервиса (формат Excel) и автоматическое создание визитов на основе этих заявок. Связывает систему управления заявками заказчика с полевым исполнением работ инженерами.

Подробное ТЗ модуля: см. файл `ТЗ-интеграция-заявок.md`.

### 11.2. Основные возможности

- **Импорт Excel** — загрузка файла с заявками (№ заявки, статус, вид оборудования, код объекта, адрес)
- **Предварительная валидация** — перед импортом выполняется проверка файла с отображением списка ошибок (номер строки, № заявки, описание ошибки). Пользователь может отменить импорт или продолжить (ошибочные строки будут пропущены)
- **Автоматический маппинг** — сопоставление кода объекта и адреса со справочником адресов
- **Группировка по объекту** — несколько заявок на один объект → один визит с несколькими задачами
- **Назначение инженеров** — с валидацией специализации по виду оборудования
- **Множественное назначение** — для заявок вида «ИСЖ объекта» (2+ инженера с разными специализациями)
- **Отказ инженера** — с указанием причины и уведомлением ТМ
- **Отчёт по номерам заявок** — формирование сводного отчёта по списку внешних номеров

### 11.3. Роли в модуле заявок

| Роль | Права |
|------|-------|
| **Администратор** | Импорт, просмотр всех заявок, назначение/снятие инженеров, отчёты |
| **ТМ** | Импорт, просмотр заявок своей территории, назначение инженеров своей группы, отчёты |
| **Инженер** | Просмотр назначенных заявок, уведомления, отказ от заявки |

### 11.4. Новые сущности данных

| Сущность | Описание |
|----------|----------|
| `imported_requests` | Импортированные заявки с маппингом на объекты и визиты |
| `visit_engineers` | Связь визит-инженер (множественное назначение) |
| `request_assignment_log` | История назначений/снятий/отказов |

### 11.5. Новый статус визита

`awaiting_assignment` — визит создан из заявок, но инженер ещё не назначен.

### 11.6. API модуля заявок

| Метод | Путь | Описание |
|-------|------|----------|
| POST | `/api/requests/import` | Импорт Excel-файла заявок |
| GET | `/api/requests` | Список заявок (с фильтрацией по роли) |
| GET | `/api/requests/:id` | Детали заявки |
| POST | `/api/requests/:id/bind` | Ручная привязка к объекту |
| POST | `/api/requests/assign` | Назначение инженера |
| POST | `/api/requests/unassign` | Снятие назначения |
| POST | `/api/requests/decline` | Отказ инженера |
| POST | `/api/requests/search-by-numbers` | Поиск по номерам для отчёта |

### 11.7. Интерфейсные страницы

| Страница | Путь | Описание |
|----------|------|----------|
| Заявки (ТМ/Админ) | `/requests` | Таблица заявок, импорт, назначение инженеров |
| Мои заявки (Инженер) | `/my-requests` | Список назначенных заявок, отказ |

---

## 12. ЭТАПЫ РАЗРАБОТКИ

| Этап | Описание | Срок |
|------|----------|------|
| 1 | Проектирование БД и API | 2 недели |
| 2 | Разработка административного модуля (справочники) | 2 недели |
| 3 | Разработка мобильного PWA (инженер) | 4 недели |
| 4 | Интеграция офлайн-режима | 1 неделя |
| 5 | Генерация PDF и ZIP | 2 недели |
| 6 | Интеграция email-отправки | 1 неделя |
| 7 | Модуль интеграции внешних заявок | 3 недели |
| 8 | Тестирование и отладка | 2 недели |
| 9 | Промышленная эксплуатация | — |

**Общий срок:** ~17 недель

---

## 13. ПРИЛОЖЕНИЯ

### Приложение А. Справочник адресов

См. файл `Требования к справочнику.md` — ~230 адресов г. Москвы и Московской области.

### Приложение Б. Справочник типовых рекомендаций

См. файл `Справочник типовых рекомендаций.md` — 38 активных категорий + 1 деактивированная, ~150 рекомендаций.

### Приложение В. Шаблон акта

См. файл `Шаблон акта.md` — структура PDF-отчета.

### Приложение Г. Ролевая модель

См. файл `Ролевая модель.txt`.

---

